spring:
  application:
    name: metroopt-backend

  datasource:
    url: jdbc:mysql://localhost:3306/metroopt_ai
    username: root
    password: Kart@17032005

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:defaultSecretKeyWithAtLeast32CharactersForDevelopment!}
  expiration: 86400000 #24 hours

gemini:
  api:
    key: ${GEMINI_API_KEY:}
    url: https://generativelanguage.googleapis.com/v1beta/models
  model: gemini-2.5-flash